import { css } from "@emotion/css";

export const messageOwner = css({
  display: "flex",
  justifyContent: "flex-end",
});
export const messageOther = css({
  display: "flex",
 justifyContent: "flex-start"
});
